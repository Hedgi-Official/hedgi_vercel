es—you need to update your database schema so that the actual table matches your code's schema definition. In your case, your updated Drizzle schema defines a broker column for the hedges table. However, the error indicates that the database table hasn't been updated accordingly.

What You Should Do
Update the Schema Code (if needed):
Ensure your Drizzle schema code is updated to include the broker column. For example, your schema should include:

ts
Copy
export const hedges = pgTable("hedges", {
  id: serial("id").primaryKey(),
  userId: serial("user_id").references(() => users.id),
  baseCurrency: text("base_currency").notNull(),
  targetCurrency: text("target_currency").notNull(),
  amount: decimal("amount", { precision: 10, scale: 2 }).notNull(),
  rate: decimal("rate", { precision: 10, scale: 6 }).notNull(),
  duration: integer("duration").notNull(), // in days
  margin: decimal("margin", { precision: 10, scale: 2 }),
  status: text("status").notNull(),
  broker: text("broker").default("tickmill"),  // <-- new column
  tradeOrderNumber: text("trade_order_number"),
  tradeStatus: text("trade_status"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  completedAt: timestamp("completed_at"),
});
Migrate or Manually Update the Table:
If you already deployed the previous version of the schema (which didn’t have the broker column), update your database so that it includes the new column. This can be done by:

Using a Migration Tool:
Generate and run a migration that executes a command like:

sql
Copy
ALTER TABLE hedges ADD COLUMN broker TEXT DEFAULT 'tickmill';
Manually:
Connect to your database using a client (such as psql, pgAdmin, or DBeaver) and run the above SQL command.

Restart Your Server:
After updating the database, restart your server. The new schema will now be in sync, and your insert operations should work without triggering the error.

Final Note
Updating your schema code alone isn’t enough—the database itself must be updated to reflect the changes in the code. In your situation, you should update your database (via migration or manually) so that the hedges table now includes the broker column as defined in your schema code.

Once these steps are completed, your server should no longer crash when trying to insert data, and your hedge data (including the broker property) will be stored properly.