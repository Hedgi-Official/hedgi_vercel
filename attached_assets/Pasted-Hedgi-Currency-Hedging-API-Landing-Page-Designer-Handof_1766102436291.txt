Hedgi Currency Hedging API Landing Page — Designer Handoff Brief (1-pager)

Page goal: convert first testers / design partners to Request Sandbox Access (primary) and View Docs (secondary).
Audience: product + engineering + treasury/ops at cross-border businesses.

1) Above-the-fold (hero)

Headline (pick one):

“Every cross-border transaction is an FX position.”

“Lock rates. Hedge exposure. Protect margin — by API.”

Subhead (must include both use-cases):
“Hedgi is a Currency Hedging API for teams that want to embed hedging in their product or hedge internal FX exposure — with developer-first REST endpoints and interactive docs.”

Primary CTA: “Request Sandbox Access” (form modal)
Secondary CTA: “View Docs” (link to api.hedgi.ai/docs) — keep visible in hero + sticky nav

Hero visual: code + mini-flow graphic

Flow: Exposure → Quote → Lock → Hedge → Webhooks/Reporting

Add a small “Works with your existing payout/settlement flows” line.

*(Hero patterns matter a lot for conversion; keep it crisp and action-oriented.) 
prismic.io

2) Credibility strip (small stats, optional)

Use 1–2 quick lines to make “FX risk is real” feel undeniable:

“FX markets trade $7.5T/day (scale + volatility).” 
Bank for International Settlements

“Cross-border payments (traditional + crypto) approached ~$1 quadrillion in 2024.” 
IMF

3) “What you get” (API capabilities, 4–6 cards)

Section title: “Hedging infrastructure you can call.”

Cards (label as API-shaped outcomes):

Get hedge quotes (price an exposure programmatically)

Lock rates & execute hedges (convert quotes → hedges)

Track positions & exposure (what’s open vs hedged)

Webhooks for lifecycle events (status updates into your system)

Reporting / exports (finance-ready summaries)

Note: Swagger-style interactive docs are a big DX trust signal; highlight “Try endpoints live in docs.” 
swagger.io

4) Solutions grid (your 4 target customer groups)

Design as 2×2 cards. Each card has: Who / Pain / Outcome / Example flow.

Remittance firms
Outcome: offer locked FX rates at checkout and protect margin.

Crypto firms (on/off-ramp ops)
Outcome: hedge FX exposure created by settlement timing + treasury conversions.

Import / export companies
Outcome: protect margin between PO → shipment → payment (payables/receivables).

Affiliate / performance marketers (foreign media spend)
Outcome: stabilize CAC/ROAS by hedging forecasted spend currency.

Each card CTA: “See example flow” (opens a simple sequence diagram).

5) “How it works” (simple 4-step)

Detect exposure

Request quote

Lock + execute hedge

Track + reconcile (webhooks + reports)

(Use icons + a thin line diagram; keep it scannable.)

6) Trust & Security (don’t bury it)

Title: “Built for financial workflows.”
Bullets (designer should reserve space for these):

API key auth + least-privilege keys

Idempotency (safe retries), rate limits

Signed webhooks, audit logs
(If any are roadmap, mark “Planned” visually.)

7) Conversion block (final CTA)

Title: “Become a launch design partner.”
Form fields (minimal): company, use case dropdown, monthly FX volume band, email.
Buttons: Request Sandbox Access + View Docs

8) Nav + footer requirements

Sticky nav: Solutions • API • Security • Docs • Contact + CTA button
Footer: compliance/legal links, email, socials, “Status” link if you have it.

Assets the designer should produce

Desktop + mobile layouts (single page)

1 “code snippet” component (dark card) + 4 sequence-diagram modals (one per vertical)

Reusable card system (features + verticals) + CTA form modal

If you paste your actual endpoint nouns (e.g., quotes, hedges, positions, webhooks) I can rewrite the Feature cards and the example flows to match your real API wording 1:1 (so the designer can design around exact labels).