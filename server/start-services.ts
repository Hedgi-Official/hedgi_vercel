import { log } from './vite';

// We're now using a direct curl-based API to broker servers
log('Using new broker API at https://digit-tricks-dense-fundamental.trycloudflare.com for trading operations');

// Dummy export to avoid breaking existing imports
export const bridgeProcess = null;